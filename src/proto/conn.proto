syntax = "proto3";

message ConnectionRequest {
    string identifier = 1;
    string ipaddr = 2;
    uint32 port = 3;
    double time = 4;
    fixed64 uid = 5;
}

message Packet {
    fixed64 uid = 1;
    bytes packet = 2;
}
